# Repositorio del curso de APIs con LLMs

Este repositorio contiene los ejemplos y proyectos del curso de APIs con LLMs.

Cada carpeta tiene un archivo `ENV_MODELO` con las variables de entorno necesarias para ejecutar los ejemplos. 
Debes copiar el contenido de ese archivo a un archivo `.env` o `.env.local` en la raíz del proyecto.
